/target
**/target
*.profraw
.DS_Store
/.claude/

# cargo-fuzz run output. The seed corpora under fuzz/corpus/ are committed;
# crash reproducers and coverage profiles are local to a run.
fuzz/artifacts/
fuzz/coverage/

# Credentials. Nothing here has ever been committed and push protection is on,
# but neither of those helps somebody who drops a local .env next to a compose
# file and runs `git add -A`. This is the net under that.
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
*.jks
*.keystore
id_rsa*
id_ed25519*
.netrc
credentials
secrets/

# Docusaurus docs site (website/)
website/node_modules
website/build
website/.docusaurus
website/.cache-loader
website/test-results
website/playwright-report
website/blob-report
# Benchmark pages rendered from the submodule by website/scripts/sync-benchmark-docs.mjs
website/.benchmarks
website/.wrangler
npm-debug.log*
yarn-error.log
