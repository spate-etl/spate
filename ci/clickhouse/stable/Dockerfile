# The newest ClickHouse release, LTS or otherwise.
#
# No Dependabot `ignore` applies, so this lane follows every release and is where
# a server change reaches CI first. It currently resolves to the same image as
# the `lts` lane, and the version matrix runs no separate job while that holds.
# ../README.md has the account.

FROM clickhouse/clickhouse-server:26.8.6.5@sha256:a73b5c0fb6f88e02357aead0906ad53882b07e4d202327489798844ba745e64d
