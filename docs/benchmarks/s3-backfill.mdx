---
title: S3 backfill baseline
sidebar_position: 9
description: End-to-end throughput of a bounded object-storage backfill — listing, streaming, decompression, framing, commit — per codec, on a local store.
---

import BenchBars from '@site/src/components/Bench/BenchBars';

export const codecLabels = {
  none: 'uncompressed',
  gzip: 'gzip',
  zstd: 'zstd',
};

# S3 backfill baseline

What does a bounded backfill cost with the network taken out of the loop?
This rig stages NDJSON objects on a **local filesystem store** and runs the
real thing end to end: the leader's listing and split planning, per-split
fetchers, streaming decompression and framing on the pipeline threads,
fenced progress commits, and the self-terminating drain. The sink is a null
writer, so the number is the **source's decode-and-frame ceiling** — a real
S3 backfill is then bounded by network and request latency, not by this
machinery.

:::note
The charts show the **coordinated** source. Rows recorded before that
rework are kept in `benchmarks/results/s3-backfill.jsonl` but filtered out
of the charts: the rig's config shape changed (`split_target_bytes`
replaced `lanes`) and so did what the run measures, so the two sets are not
comparable and are not presented as a before/after.
:::

Because the job is bounded, the honest metric is total wall time for the
whole job, not a steady-state window: every figure below includes listing
and the final synchronous commit.

<BenchBars
  bench="s3_backfill"
  metric="records_per_s"
  category="codec"
  where={{ split_target_mb: 64 }}
  labels={codecLabels}
  title="Backfill throughput by codec"
  caption="Records through the full pipeline per second of end-to-end wall time (64 objects × 20k records × 256 B). Higher is better."
/>

<BenchBars
  bench="s3_backfill"
  metric="decoded_mb_per_s"
  category="codec"
  where={{ split_target_mb: 64 }}
  labels={codecLabels}
  title="Decoded bytes per second"
  caption="Decompressed NDJSON bytes framed per second. Compressed objects come out ahead here because far fewer stored bytes cross the store — the decompressors are cheaper than the extra reads they save. Lower stored volume is also what a real S3 backfill wants: fewer bytes over the network."
/>

<BenchBars
  bench="s3_backfill"
  metric="stored_mb_per_s"
  category="codec"
  where={{ split_target_mb: 64 }}
  labels={codecLabels}
  title="Stored bytes per second"
  caption={<>The same job measured at the other end: on-disk bytes — post-compression, what a real backfill would pull over the network — read per second. This is the chart above's argument made visible; the two differ only by the codec's compression ratio. Read the direction with care: the bars are drawn longer-is-higher, but a <em>short</em> bar is the good outcome here, because it means the same records arrived having moved two orders of magnitude fewer bytes.</>}
/>

## Reading the numbers

- **No GO/NO-GO gate.** This is a new capability, not an alternative
  implementation competing against an incumbent; the baseline exists so
  regressions in the source's hot path (framing, decompression, the
  chunk channel) show up as a falling bar.
- **Compressed beats plain on a local store.** gzip and zstd read far fewer
  stored bytes (the ratio is the payload's compression ratio — this rig's
  synthetic records compress by two orders of magnitude; real-world JSON
  compresses less) and pay a modest decompress cost on the pipeline
  threads. Against real S3 the effect is stronger — stored bytes are network
  bytes.
- The rig: `benchmarks/src/bin/s3_backfill.rs` (`OBJECTS`,
  `RECORDS_PER_OBJECT`, `PAYLOAD`, `SPLIT_TARGET_MB`, `THREADS`, `CODECS` env knobs).
  Results append to `benchmarks/results/s3-backfill.jsonl` via `RESULTS`.
