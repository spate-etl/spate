# Your first pipeline: Kafka → Avro → ClickHouse

This tutorial builds the flagship shape: consume Confluent-framed Avro from
Kafka, validate and transform records in an operator chain, and write them
to ClickHouse in large batches, at-least-once and drained gracefully on
SIGTERM. It is
[`kafka_avro_to_clickhouse.rs`](repo:crates/spate/examples/kafka_avro_to_clickhouse.rs)
and the YAML beside it
([`kafka_avro_to_clickhouse.yaml`](repo:crates/spate/examples/kafka_avro_to_clickhouse.yaml)).
Every Rust block below is rendered from that source, so what you read is
what compiles.

You need Kafka, a Confluent-compatible schema registry, and ClickHouse, plus
the `full` feature (see [Installation](01-installation.mdx)). From the
repository:

```sh
cargo run --release -p spate --example kafka_avro_to_clickhouse --features full
```

## 1. Create the target table

Before running anything, create the table, and note the deduplication
window setting:

```sql
CREATE TABLE orders (
    order_id    UInt64,
    customer_id UInt32,
    region      LowCardinality(String),
    placed_at   DateTime64(3),
    total_cents UInt64
) ENGINE = MergeTree ORDER BY order_id
  SETTINGS non_replicated_deduplication_window = 100;
```

Two of those column types do not spell their Rust field the same way, and both
are deliberate. `LowCardinality(String)` takes a plain `String`. The encoding
is the server's business, not the row's. `DateTime64(3)` takes the
`DateTime64Millis` wrapper in step 2, which is what declares the scale; a bare
`i64` would encode identically and validate against nothing. `UInt32` does
spell its field, and that is its own constraint: the width has to match rather
than merely be large enough.

:::warning
On plain `MergeTree`, insert deduplication **silently no-ops** unless the
table sets `non_replicated_deduplication_window` greater than 0 (the
server default is 0). Without it, the sink's dedup tokens do nothing and
a retried batch lands twice. `Replicated*MergeTree` tables keep a window
by default and don't need the setting. Even with the window, dedup
tokens only cover same-batch retries, not crash replay. Read
[Delivery guarantees](../02-concepts/02-delivery-guarantees.mdx) before
choosing a table engine for production.
:::

## 2. The record types

The writer schema is what your producer registers and what the records on the
topic are framed against. The pipeline fetches it by id and never declares it:

```json
{
  "type": "record", "name": "OrderPlaced", "namespace": "spate.datagen",
  "fields": [
    {"name": "order_id", "type": "long"},
    {"name": "customer_id", "type": "int"},
    {"name": "region", "type": "string"},
    {"name": "placed_at",
     "type": {"type": "long", "logicalType": "timestamp-millis"}},
    {"name": "lines", "type": {"type": "array", "items": {
      "type": "record", "name": "OrderLine", "fields": [
        {"name": "sku", "type": "string"},
        {"name": "qty", "type": "int"},
        {"name": "unit_cents", "type": "int"}
      ]}}}
  ]
}
```

The record is the `order_placed` event of the storefront stream the examples
share, field for field with the one
[`spate-datagen`](https://docs.rs/spate-datagen) mints. It is not a source you
can point at this topic. That crate is a library with no producer of its own,
and it writes bare datums against a top-level union of the three event kinds
rather than the Confluent frames this pipeline reads. The shape carries over
between them; the bytes do not.

`Deserialize` reads `OrderPlaced` from the writer schema above, and
`OrderLine` is the nested element its `lines` array holds. `Serialize` writes
`OrderRow` as RowBinary. `OrderPlaced` and `OrderRow` are the pipeline's ends,
and the chain's `try_map` in step 4 turns the first into the last, totaling
the lines on the way:

```rust file=crates/spate/examples/kafka_avro_to_clickhouse.rs region=record
```

:::warning
RowBinary carries no column names, so **`OrderRow`'s field declaration
order must match the `columns` list in the YAML**. Order is the wire
contract. The `validate_schema` option below catches mismatches against
the live table at startup instead of at 3 a.m.
:::

## 3. The YAML

Tuning lives in YAML next to the binary; the operator graph never does.
Every `${VAR:-default}` interpolates from the environment (in Kubernetes:
ConfigMap/Secret env). Framework sections (`pipeline`, `admin`, `checkpoint`,
`backpressure`, `metrics`) are typed and validated; the `source`,
`deserializer`, and `sink` bodies belong to their connectors:

```yaml
pipeline:
  name: orders
  # threads: 4          # default: available cores minus the I/O reserve
  io_threads: 2

checkpoint:
  interval: 5s
  max_pending_batches: 1024
  drain_timeout: 25s    # keep below terminationGracePeriodSeconds

backpressure:
  # Sizing rule (see the Backpressure concept page): the budget's low watermark
  # must hold ~2x (shards x inflight x batch.max_bytes + queued chunks).
  max_inflight_bytes: 1GiB

admin:
  listen: 0.0.0.0:9090  # /metrics, /healthz, /readyz

metrics:
  exporter: prometheus

source:
  kafka:
    brokers: ${KAFKA_BROKERS:-localhost:9092}
    topic: ${KAFKA_TOPIC:-orders}
    group_id: ${KAFKA_GROUP:-orders-etl}
    commit_interval: 5s
    rdkafka:
      # Raw librdkafka passthrough. Properties the framework owns for
      # correctness (offset storage, auto-commit) are rejected here.
      auto.offset.reset: earliest

deserializer:
  avro:
    mode: confluent
    registry:
      url: ${SCHEMA_REGISTRY_URL:-http://localhost:8081}
    prewarm_subjects: ["${KAFKA_TOPIC:-orders}-value"]

sink:
  clickhouse:
    table: ${CLICKHOUSE_TABLE:-orders}
    # Column order is the RowBinary wire contract: it must match the row
    # struct's field declaration order.
    columns: [order_id, customer_id, region, placed_at, total_cents]
    shards:
      - replicas: ["${CLICKHOUSE_URL:-http://localhost:8123}"]
    user: ${CLICKHOUSE_USER:-default}
    password: ${CLICKHOUSE_PASSWORD:-}
    # Fail startup (and the first record) when the columns, row struct,
    # and live table disagree: off | names | full.
    validate_schema: full
    batch:
      max_rows: 500000
      max_bytes: 128MiB
      linger: 1s
    inflight:
      max_per_shard: 2
```

With a saturated source, an undersized budget collapses throughput. The
sizing rule quoted in that `backpressure` comment, and the arithmetic behind
it, are in [Backpressure](../02-concepts/03-backpressure.mdx).

## 4. The binary

The builder owns the process plumbing (telemetry, metrics exporter, the
shared I/O runtime, shard queues, sink workers, probes). The code you write
is only what is specific to *this* pipeline: connector construction, schema
validation, and the chain.

The imports are the framework prelude, one line per connector, and the serde
derives the record types in step 2 need:

```rust file=crates/spate/examples/kafka_avro_to_clickhouse.rs region=imports
```

That leaves `main`, in full:

```rust file=crates/spate/examples/kafka_avro_to_clickhouse.rs region=assembly
```

Walking the assembly:

- **`Pipeline::from_path`** loads the YAML and owns init: JSON logs
  (`RUST_LOG` overrides the filter; call `spate::telemetry::init` first to
  customize), the metrics exporter (installed before any handle can exist),
  and the shared I/O runtime. Point `SPATE_CONFIG` elsewhere to reconfigure
  without recompiling.
- **`pipeline.io_handle()` / `pipeline.block_on(..)`** give connectors the
  I/O runtime *before* `run`. The schema-registry fetcher and the
  `validate_schema` pre-flight both use it.
- **`.sink(sink)`** spawns the sharded sink workers and wires the drain and
  the `/readyz` probe. The single `sink:` section above is addressed as
  `"default"`; a pipeline with several named sinks resolves each by name.
- **`.chains(..)`** installs the per-thread chain factory. The `try_map`
  totals an order's lines into its row; an order carrying none cannot be
  totaled, so `ErrorPolicy::Skip` drops it, counts it on
  `spate_operator_records_dropped_total`, and the chain keeps going. See
  [Error handling](../02-concepts/04-error-handling.mdx).
- **`.run(source)`** blocks until SIGTERM/SIGINT (drain) or a fatal error,
  then returns an exit report.

Each step is a thin composition of public primitives you can drop down to.
The full mapping is in the `spate::pipeline::Pipeline` module docs and in
[Manual assembly](../03-guides/manual-assembly.mdx).

## 5. Run it, watch it, stop it

```sh
export KAFKA_BROKERS=localhost:9092
export SCHEMA_REGISTRY_URL=http://localhost:8081
export CLICKHOUSE_URL=http://localhost:8123
cargo run --release -p spate --example kafka_avro_to_clickhouse --features full
```

Probes and metrics are on the admin listener from the YAML:

```sh
curl localhost:9090/readyz    # assignment received and sink connected
curl localhost:9090/healthz   # poll loop alive, watermarks moving
curl localhost:9090/metrics   # Prometheus scrape endpoint
```

**On SIGTERM.** Send the process SIGTERM (what Kubernetes does on
pod termination) and it drains instead of dying. Lanes stop polling, chains
flush, sink batches complete, bounded by `checkpoint.drain_timeout`, and
offsets commit synchronously before the process exits. If the sink is down
at the deadline, unflushed batches are abandoned *loudly* (metric + log)
and replay on restart, so at-least-once holds either way. Keep `drain_timeout`
below your pod's `terminationGracePeriodSeconds` so the drain finishes
before the SIGKILL. Details in
[Graceful shutdown](../03-guides/graceful-shutdown.mdx).

## Where next

- [Delivery guarantees](../02-concepts/02-delivery-guarantees.mdx) — what
  at-least-once buys you and what it doesn't.
- [Kafka](../04-connectors/sources/kafka/README.mdx), [Avro](../04-connectors/formats/avro/README.mdx), and
  [ClickHouse](../04-connectors/sinks/clickhouse/README.mdx) connector references.
- [Docker](../05-deployment/docker.mdx) and
  [Monitoring](../05-deployment/monitoring.mdx) to take this to production.
