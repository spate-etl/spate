{/*
  Scaffold for a new connector page. Copy this file to
  04-connectors/<role>/<name>/README.mdx and fill it in.

  Underscore-prefixed, so Docusaurus does not render it as a page.

  The rules behind this shape are in docs/STYLE.md §3. The two things to get
  right before anything else:

    - This page names one vendor: its own. Link to other connectors, never
      explain them.
    - Reference voice: describe the system, not the reader. "The sink retries",
      not "you get a retry".

  Delete every section that does not apply, but do not reorder the ones that
  remain, and do not rename them. `sources/kafka/README.mdx` is the worked
  reference if you want to see it filled in.
*/}

# `NAME` (source | sink | format | coordination store)

The `NAME` ROLE (`spate-NAME`, feature `NAME` on the `spate` facade) does
ONE-PARAGRAPH-SUMMARY. Say what it connects to, the one design decision a
reader most needs to know up front, and link to the relevant `docs/adr/` record if there is
rationale worth reading.

Construct it from the pipeline's opaque section:

{/*
  This fence is a placeholder: `SECTION` and `NameThing` name nothing, so it
  cannot compile, and it claims docs/STYLE.md §10's third exemption for exactly
  as long as it stays a scaffold. A real page's Rust is rendered from a
  compiled source instead — leave the fence empty and put
  `file=crates/spate/examples/<example>.rs region=<name>` on the info string,
  with `// ANCHOR: <name>` and `// ANCHOR_END: <name>` around those lines in the
  example. Say once in prose which example it is and how to run it; the fence
  itself renders with no header, and `title="…"` on one fence is how a page
  drawing on two sources marks which is which.
*/}

```rust
let x = NameThing::from_component_config(&pipeline.config().SECTION)?;
```

## Configuration

The `SECTION: { NAME: ... }` section deserializes into `NameConfig`
([`spate-NAME/src/config.rs`](repo:crates/spate-NAME/src/config.rs));
unknown fields are rejected with the offending key.

```yaml
SECTION:
  NAME:
    key: value
```

| Key | Type | Default | Description |
|---|---|---|---|
| `key` | string | `required` | What it does. |
| `other` | duration | `5s` | Defaults are one of exactly three forms: `required`, a backticked literal, or `none`. Never prose. |

{/*
  Framework-owned sink-pool keys (batch/inflight/retry/breaker) do NOT belong
  in this table. Link to the appendix instead:
  ../../../07-reference/configuration.mdx#sink-pool
*/}

## The `PASSTHROUGH` passthrough and the denylist

{/* Only if the connector has a raw passthrough. If it has one, it needs this
    section — a table row on its own is not enough. */}

Why the passthrough exists, and that properties the framework owns for
correctness are rejected at load time with an explanation.

| Denied property | Why |
|---|---|
| `some.property` | Which framework guarantee overriding it would break. |

## Security (TLS, mTLS, SASL)

{/* MANDATORY if the connector takes credentials of any kind. Link the hub. */}

Connector-specific auth. Prose shared with another page goes in an MDX partial
(`_name.mdx`) imported by both — and rendered at the same position in both.

See [Securing connections](../../../03-guides/securing-connections.mdx) for the
generic model (secrets via `${VAR}`, auth failure is fatal).

## BEHAVIORAL SECTION

{/* As many as the connector needs: delivery semantics, backpressure, offsets,
    error handling, teardown. Sentence-case headings. */}

## Metrics

{/* The families this connector registers, with their labels. This page is
    their only home — docs/METRICS.md carries the taxonomy and the framework
    families, not connector registries. */}

Registered through a `Meter` under the `NAME` namespace, so every family is
prefixed `spate_NAME_`. Standard labels (`pipeline`, `component`,
`component_type`) are on all of them; extras are noted per family.

| Metric | Type | Extra labels | Description |
|---|---|---|---|
| `spate_NAME_thing_total` | counter | — | What it counts. |

See [Metrics](../../../../METRICS.md) for the taxonomy, the reserved roots and
the series-ownership rules.

## Related

- [Page](path.mdx) — every entry carries an em-dash gloss saying why to follow
  it. A bare link list is not a Related section.
