# ClickHouse multi-table split

One pipeline can write to **several ClickHouse tables**, routing each record to
the table for its kind — each table with its own columns, encoder, shard router,
and part tuning. This is the connector-specific side of the
[multi-sink split](../../../02-concepts/06-multi-sink.mdx) concept: the split
terminal is generic; here is how the pieces line up for ClickHouse.

It applies to a stream carrying genuinely different record types, each wanting
its own columns and `ORDER BY`: the pipeline fans them out instead of a `Null`
table with one `MATERIALIZED VIEW` per type. The trade is pipeline-side fan-out
CPU and part control against server-side fan-out.

## Configuration: a `sinks:` map

Replace the single `sink:` section with a `sinks:` map keyed by name. Each entry
is a full, ordinary ClickHouse sink section — its own `table`, `columns`,
`format`, `shards`, and `batch`/`inflight` — so per-table part sizing is tuned
independently. The map key is the name the chain resolves with `ctx.sink(...)`.

```yaml
sinks:
  gauge:
    clickhouse:
      table: metrics_gauge
      columns: [host, ts_ms, name, value]   # this table's own columns/order
      format: native
      shards:
        - replicas: ["${CLICKHOUSE_URL:-http://localhost:8123}"]
      batch: { max_rows: 500000, linger: 1s }

  text:
    clickhouse:
      table: metrics_text
      columns: [host, ts_ms, name, text]    # a different schema
      format: native
      shards:
        - replicas: ["${CLICKHOUSE_URL:-http://localhost:8123}"]
      # A low-volume table: linger longer to build bigger parts (at the cost of
      # checkpoint lag — see the tuning note below).
      batch: { max_rows: 500000, linger: 5s }
```

`sink:` and `sinks:` are mutually exclusive; a single-sink pipeline keeps using
`sink:` (addressed as `"default"`). See the
[configuration reference](../../../07-reference/configuration.mdx#sinks). Every key
documented for a single [ClickHouse sink](README.mdx#configuration) applies
per-entry.

## Wiring: one sink, encoder, and router per table

Each table is an ordinary `ClickHouseSink`, built from its named config section,
minting its own Native encoder (from *its* `system.columns`) and its own shard
router before `add_sink(name, ...)` moves it into its worker pool. That is the
single-sink wiring, N times. The whole program is
`crates/spate/examples/multi_table_split.rs`; it needs a Kafka topic of Avro
`MetricBatch` messages and both target tables, and runs with
`cargo run --release -p spate --features full --example multi_table_split`:

```rust file=crates/spate/examples/multi_table_split.rs region=assembly
```

`gauge_sink` and `text_sink` come from the `sinks:` map's `gauge` and `text`
entries, resolved by `sink_config(name)`. Each mints a `NativeEncoder` over the
row family that table receives — `Owned<GaugeRow>` and `Owned<TextRow>`, two
structs with different columns — from its own fetched schema, and a
`DistributedRouter` over the same family from the `ShardKey` extractors
`gauge_key` and `text_key`, which shard both tables on `host`. The split's
input family is `Owned<Sample>`, the enriched reading `flat_map` produces from
each decoded `Owned<MetricBatch>`. See the concept page for the
[routing DSL walkthrough](../../../02-concepts/06-multi-sink.mdx#the-routing-dsl).

## What carries over per table, unchanged

Each branch is a full ClickHouse sink, so everything on the
[ClickHouse sink page](README.mdx) holds **per table**:

- **Native (or RowBinary) encoding on pipeline threads**, positional against
  that table's `columns`. Mixed formats across tables are fine — each sink
  chooses its own.
- **Direct-to-shard writes** with `wait_end_of_query=1`; a table's write is its
  own durable-ack point. The split holds the source batch until *every* table it
  touched has acked (see [delivery](../../../02-concepts/06-multi-sink.mdx#at-least-once-across-destinations-is-free)).
- **Per-batch `insert_deduplication_token`** per table. The same
  [window warning](README.mdx#deduplication-tokens--and-the-window-warning)
  applies: set `non_replicated_deduplication_window` on **every** target table,
  or retries duplicate silently.
- **Replica rotation, circuit breaker, readiness probe** per table. `/readyz`
  reports connected only when every table's replicas probe healthy.
- **Metrics per table**: each sink's `spate_sink_*` series carry the sink name as
  their `component` label, so `spate_sink_records_total{component="gauge"}` and
  `{component="text"}` are distinct.

## Per-table part tuning — and the checkpoint-lag cost

The whole point of splitting in the pipeline is per-table part control: each
table's `batch.max_rows`/`max_bytes`/`linger` is sized for *its* volume. A hot
table seals on rows in milliseconds; a rare table would trickle, so a longer
`linger` builds bigger parts for it.

:::info
`linger` on a rare table is **not free**: because the source watermark is
held until every table a batch touched has flushed, a long rare-table
`linger` holds the offsets of every source batch that carried even one rare
record — inflating checkpoint lag and restart replay. The framework bounds
the *pre-batch* half of that hold: a rare table's below-target chunk
buffer is sealed on every commit tick, so worst-case checkpoint lag is
`checkpoint.interval` (chunk seal) + that table's `linger` (batch close) +
the write. The `linger` half is sized in config: sealing on a modest
`max_rows` keeps `linger` biting only in genuine lulls. (A *pending* batch never
trips `checkpoint.stalled_fail_after` — that watchdog fires only when a
batch has permanently **failed**.) The full reasoning — including
segregating the source by kind to dissolve the tension — is in the
[concept page's trade-off section](../../../02-concepts/06-multi-sink.mdx#the-trade-write-size-vs-checkpoint-lag).
:::

## `distributed_check` per table

Each table can carry its own [`distributed_check`](distributed-parity.mdx): the
router placement is verified against that table's `Distributed` DDL
independently, so a sharded multi-table deployment keeps every table aligned
with its cluster.

## Related

- [Multi-sink split](../../../02-concepts/06-multi-sink.mdx) — the concept, the routing DSL, and the at-least-once and write-size trade.
- [ClickHouse sink](README.mdx) — everything a single sink does, which each branch inherits.
- [Configuration reference](../../../07-reference/configuration.mdx#sinks) — the `sinks:` map.
