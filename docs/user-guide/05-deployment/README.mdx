---
description: "Packaging, monitoring, tuning, and scaling a pipeline into a deployed, replicated fleet."
---

# Deployment

Everything after `cargo build`. Your pipeline compiles to a single binary
that reads one YAML file, serves its own probes and metrics, and drains
cleanly on SIGTERM. Deployment is packaging that binary, watching it, and
sizing it.

## [Docker](docker.mdx)

Packaging a pipeline binary into a minimal, non-root image, using the
flagship image ([`examples/docker/Dockerfile`](repo:examples/docker/Dockerfile))
as the template: the
`SPATE_CONFIG` mount pattern, the `/readyz`, `/healthz`, and `/metrics`
endpoints on the admin server, and aligning the SIGTERM grace period with
the drain timeout.

## [Monitoring](monitoring.mdx)

The metrics model: the `metrics` facade as the one instrumentation API, the
Prometheus exporter on the admin server, label conventions, the hot-path
counting discipline, and a small set of starting alerts. The full metric
taxonomy lives in [Metrics](../../METRICS.md).

## [Tuning](tuning.mdx)

Sizing a pipeline: thread counts, batch thresholds, in-flight limits, the
backpressure budget and its sizing rule, queue capacities, checkpoint
timing, and core pinning, with the exact defaults and why each knob moves
what it moves.

## [Scaling out](scaling-out.mdx)

Several identical replicas dividing one source's work: brokered sources that
divide it natively, and the coordination layer for those that cannot. Covers
Kubernetes patterns (plain Deployments, pod-name instance ids), lease tuning,
and the failure-mode table.

## Related reading

- [Graceful shutdown](../03-guides/graceful-shutdown.mdx) — the drain
  choreography these pages configure.
- [Backpressure](../02-concepts/03-backpressure.mdx) — the mental model
  behind the budget knobs in [Tuning](tuning.mdx).
- [Configuration reference](../07-reference/configuration.mdx) — every YAML
  key, type, and default in one place.
- [Decision records](../../adr/README.mdx) — the rationale behind the operational
  model (one process, one pipeline; scrape-based metrics; drain-on-SIGTERM).
