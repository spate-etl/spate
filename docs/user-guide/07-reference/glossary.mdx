# Glossary

Precise definitions of the terms used throughout this guide, in
alphabetical order. Where a term names a public type, the exact contract is
in the rustdoc ([Reference](README.mdx)); where it names a mechanism, the
rationale is in the [decision records](../../adr/README.mdx).

### Ack / `AckRef`

The acknowledgment handle for one source poll batch: a clone of a
refcounted `Arc` created **per batch**, not per record. Every record
derived from the batch clones it. `flat_map` children share the parent's, and
multi-output routing clones per output with worst-status merge. Dropping a
record resolves its share, and an intentional drop (a `filter`, a Skip
policy) counts as **success**. When the last clone drops, the batch's
`(partition, seq, status)` reaches the checkpointer over an unbounded
channel, so the ack path can never block behind data.

### `AckSet`

A collection of acknowledgment handles on the sink path (encoded chunks,
worker ledgers, batch accumulators) with the **opposite** drop default:
where a lone record's drop means delivered, an `AckSet` **fails** its
handles on drop and delivers only explicitly, after a durable write. This
is the fail-by-default contract that turns any teardown (a dropped queue,
an aborted worker) into a watermark stall (replay) instead of a silent
commit of unwritten data.

### Backpressure watermarks (high / low)

The hysteresis thresholds on the in-flight byte [budget](#budget--in-flight-bytes):
sources pause when usage crosses `high_ratio` × budget and may resume once
it falls below `low_ratio` × budget and at least `min_pause` has elapsed.
Unrelated to the [checkpoint watermark](#watermark), which is the same word
for a different mechanism. See [Backpressure](../02-concepts/03-backpressure.mdx).

### Budget / in-flight bytes

The global cap (`backpressure.max_inflight_bytes`) on bytes admitted into
the pipeline but not yet durably written. Charged when records enter the
terminal stage's chunks, released on durable write or failure; current
usage is `spate_backpressure_inflight_bytes`.

### Chain

The operator pipeline one pipeline thread runs: deserializer →
`map`/`filter`/`try_map`/`flat_map` stages → the terminal sink stage
(encode, chunk, route). Statically composed and monomorphized into one
loop; type erasure happens exactly once, at the chain boundary, with one
virtual call per batch. Built by the chain factory you pass to
`Pipeline::chains`, once per pipeline thread.

### Chunk

The handoff unit between a pipeline thread and a shard worker: rows encoded
(by the `RowEncoder`) into a frame targeting `ChunkConfig::target_bytes`
(default 64 KiB), carrying its rows' acks as an [`AckSet`](#ackset).
A chunk seals when it reaches the target, on every commit tick
(`checkpoint.interval`), or on an idle lull, so while the pipeline is
unblocked, a slow-filling buffer holds its acks for at most ~one checkpoint
period. Workers merge chunks into
[sealed batches](#sealed-batch), so chunk size does not bound insert size.

### Component / `component_type` labels

Two of the three standard labels on every framework metric (with
`pipeline`): `component` identifies the instance (e.g. `orders_source`,
`main.deserializer`), `component_type` the implementation, which for a
connector is the system it talks to and for an operator is its kind
(`map`, `filter`). See
[Monitoring](../05-deployment/monitoring.mdx) and
[Metrics](../../METRICS.md).

### Contiguity tracker

The checkpointer's per-partition, per-[epoch](#epoch) bookkeeping: a ring
of outstanding batch sequence numbers, from which it pops the contiguous
acknowledged **prefix** and advances the committable
[watermark](#watermark). Out-of-order acks park until the gap before them
closes, which is what makes "never commit past unacknowledged data"
structural. A synchronous, tokio-free, loom-tested module.

### Dedup token

A deterministic string minted per [sealed batch](#sealed-batch)
(`SealedBatch::dedup_token`) and reused unchanged across retries of that
batch. A sink whose destination supports insert deduplication hands the token
over to make **same-boundary retries** idempotent. It does not cover crash
replay, because after restart data re-batches with new boundaries and new tokens
([Delivery guarantees](../02-concepts/02-delivery-guarantees.mdx)).

*Connector mapping:*
[ClickHouse sink](../04-connectors/sinks/clickhouse/README.mdx#deduplication-tokens-and-the-window-warning)
— honored, but silently inert without a server-side dedup window ·
[Kafka sink](../04-connectors/sinks/kafka/README.mdx#delivery-semantics)
— unused; there is no destination-side dedup window to hand it to.

### Drain

The choreography that quiesces data without losing acknowledgments: stop
the affected lanes, flush the chains, force-seal and write out in-flight
sink batches under `checkpoint.drain_timeout`, then commit synchronously.
Triggered identically by SIGTERM (all lanes) and by a rebalance revocation
(the revoked lanes), via a `DrainBarrier` the owning pipeline threads
arrive at. Batches still unwritten at the deadline are abandoned loudly and
replay after restart.

### Epoch

Several generations share the name, all monotonic, all fencing something:
the **assignment epoch** in the checkpointer, bumped on every rebalance
(`Checkpointer::begin_epoch`) so acks issued under a previous assignment
are discarded; the **lease epoch** (`LeaseEpoch`) on a coordinated
[split](#split), bumped on every ownership change so a stale owner's
commit loses its compare-and-swap; and the **plan generation**, bumped by
every newly elected coordination leader so a deposed leader's plan write
is rejected.

### Fencing token

A monotonically increasing number a writer must present with a write, so
that a writer whose authority lapsed (an expired lease, a paused process)
is *rejected by the store* rather than trusted. In coordinated sources
the token is the split record's revision/epoch, and the fenced write is
the progress commit itself: a fenced commit writes **nothing**. Leases
alone cannot provide this; only the store-side check can.

### Lane

The data-plane unit of a source (`SourceLane`): one pollable stream pinned
to one pipeline thread, yielding borrowed payload batches. Lanes map m:n
onto pipeline threads. In a coordinated source, lanes are purely local: the
`CoordinationDriver` materializes one lane per in-flight [split](#split) and
renumbers them per assignment cycle. `spate_source_lanes_active` counts them.

*Connector mapping:*
[Kafka source](../04-connectors/sources/kafka/README.mdx#lanes-partitions-on-pipeline-threads)
— one partition queue ·
[S3 source](../04-connectors/sources/s3/README.mdx#sizing-splits-against-pipelinethreads)
— one in-flight split ·
[Datagen source](../04-connectors/sources/datagen/README.mdx#configuration) —
one generator, `partitions` of them.

### Lease

A time-bounded claim on one [split](#split): a TTL'd key in the
coordination store that the owner rewrites (heartbeats) at a third of the
lease duration. A dead owner stops heartbeating and the lease expires,
releasing the split to the fleet; a live owner that cannot reach the
store self-fences after one full lease. Leases provide *liveness*;
correctness comes from the [fencing token](#fencing-token).

### Quarantine

Where poison [splits](#split) go: a split whose tenancies keep ending in
failure consumes a delivery attempt per non-graceful end and is parked at
the cap, where it stays visible, is never re-offered, and blocks `AllComplete`
(a bounded job with quarantined work ends stalled, not falsely complete).

### Split

The leasable work unit of a coordinated source, enumerated by the
source's `SplitPlanner`. Deterministic id, opaque descriptor, weight hint;
progress commits are per-split, fenced, and resumable, so a taken-over split
continues from its last committed watermark. Distinct from a [lane](#lane)
(local execution) and a [partition](#partition) (checkpoint bookkeeping).

*Connector mapping:*
[S3 source](../04-connectors/sources/s3/README.mdx#split-identity-and-drift) —
a bin-packed batch of whole objects, identified by its members' keys and ETags.
A database source would instead plan an id range.

### Assignment

How a coordinated fleet stays balanced: the elected leader computes which
[splits](#split) every live worker should hold and publishes one
`assign.{instance}` record per worker; each worker reconciles toward its
own record, claiming what is named and cooperatively giving up what is
not. Workers never select work for themselves. Balance is on split
*weight* (not count), and sticky. A split whose owner is alive stays put
unless moving it strictly reduces imbalance, because every move costs a
[revocation](#cooperative-revocation).

An assignment is an *instruction, not a fence*: it says what a worker
should hold, never what it may write. Ownership and safety come from the
[lease](#lease) and the [fencing token](#fencing-token), so two leaders
publishing contradictory assignments still cannot produce two owners. The
normative spec is [Work assignment](../02-concepts/08-work-assignment.mdx).

### Cooperative revocation

How a split leaves a live worker: it disappears from that worker's
[assignment](#assignment), and the worker stops the split's intake at a
safe boundary, lets everything it already emitted reach the sink and
acknowledge, commits the final watermark, and releases the split. The next
owner resumes from that commit, covering everything the previous one
emitted, with zero replay.

`drain_deadline` bounds it. A source that declines to stop at a safe
boundary, or a drain still running at the deadline, has its release
**forced** instead: the split goes back anyway and its uncommitted tail
replays under the next owner, bounded by one commit interval. Forcing
keeps one wedged drain from pinning a rebalance open; the leader's
revocation is a decision, not a request.

If the leader takes the revocation back mid-drain, it is **cancelled** and
the deadline no longer applies: nothing is waiting for the split, so a slow
drain gets to finish cleanly. The drain is then bounded by silence instead:
committing nothing for `drain_deadline` releases it anyway, so a wedged
drain cannot leave a split owned with nothing reading it. See
[Work assignment](../02-concepts/08-work-assignment.mdx#revocation).

### Partition

The source-side unit of ordering and checkpointing (`PartitionId`).
Watermarks, epochs, and the contiguity tracker are all per-partition.

*Connector mapping:*
[Kafka source](../04-connectors/sources/kafka/README.mdx) — literally the topic
partition ·
[S3 source](../04-connectors/sources/s3/README.mdx#offsets-progress-resume) —
one per [split](#split), with the offset packing member ordinal and record
index ·
[Datagen source](../04-connectors/sources/datagen/README.mdx#configuration) —
one per generated lane, set by `partitions`.

### Pipeline thread

One of the N plain `std::thread`s (optionally core-pinned) running the hot
path: poll lanes → deserialize → operator chain → route to shard queues.
All CPU work happens here, with zero per-record allocations; I/O lives on
the tokio [I/O runtime](../02-concepts/01-architecture.mdx) instead.

### Replica

One endpoint of a [shard](#shard) (`[shard][replica]` in a sink topology).
Shard workers rotate writes round-robin across healthy replicas, skipping
open circuit breakers; a failed write retries the same sealed batch on the
next healthy replica.

### Sealed batch

The sink's write, retry, and acknowledgment unit (`SealedBatch`):
concatenated pre-encoded frames plus row/byte counts, the
[dedup token](#dedup-token), and the covered acks. A shard worker seals
when `batch.max_rows`, `batch.max_bytes`, or `batch.linger` trips, then
writes it via the `ShardWriter`, whose `Ok` is the durable-ack point.

### Shard

One partition of the sink's write topology, served by one worker task that
batches, seals, and dispatches up to `inflight.max_per_shard` concurrent
writes across its [replicas](#replica). Records route to shards by key
hash (or by source partition when keyless).

### Watermark

The per-partition committable position: the offset **one past the last**
contiguously acknowledged record. Stored on advance, committed to the
source on `checkpoint.interval`, and committed synchronously on drain. The
at-least-once invariant is that it never passes unacknowledged data; a
failed batch stalls it (alert on `spate_checkpoint_watermark_age_seconds`)
rather than being skipped. Distinct from the
[backpressure watermarks](#backpressure-watermarks-high--low).
