import type {SidebarsConfig} from '@docusaurus/plugin-content-docs';

// The benchmark pages, rendered from the submodule by
// scripts/sync-benchmark-docs.mjs. Explicit rather than autogenerated: the
// numbers come first, then how they were produced, then what they do not cover,
// so a reader who stops after the first page has still met the caveat.
const sidebars: SidebarsConfig = {
  benchmarks: [
    {type: 'link', label: 'Results', href: '/benchmarks'},
    'workload',
    {
      type: 'category',
      label: 'The fairness contract',
      link: {type: 'doc', id: 'contract/rules'},
      items: ['contract/envelope', 'contract/measurement', 'contract/comparability'],
    },
    'environments',
    'limitations',
    'roadmap',
    'reproduce',
  ],
};

export default sidebars;
